; ============================================================
; rtp.conf — RTP Media Port Range
; ============================================================
[general]
rtpstart=10000
rtpend=10100
strictrtp=no
probationpackets=4
